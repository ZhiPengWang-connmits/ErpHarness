# 环境配置与运行指南

> 项目名称：基于Harness Engineering架构的多智能体架构的企业采购智能助手
> 适用版本：`ErpHarness`（Python AI Agent项目） + `MotorcyclePartsProcurementSystem`（Java ERP 后端）

---

## 目录

1. [项目总览](#一项目总览)
2. [环境要求总表](#二环境要求总表)
3. [第一部分：Java ERP 后端](#三第一部分java-erp-后端)
4. [第二部分：Python AI 项目](#四第二部分python-ai-项目)
5. [外部依赖服务说明](#五外部依赖服务说明)
6. [完整启动顺序](#六完整启动顺序)
7. [端口速查表](#七端口速查表)
8. [常见问题排查](#八常见问题排查)

---

## 一、项目总览

本项目由两套独立系统组成，通过 HTTP 互相通信：

```
┌─────────────────────────────┐        ┌──────────────────────────────┐
│   Python AI 项目             │  HTTP  │   Java ERP 后端               │
│   erp_harness01/             │ ─────▶ │   MotorcyclePartsProcurement │
│                             │  /api  │   System（提供业务数据）       │
│  ┌───────────────────────┐  │        └──────────────┬───────────────┘
│  │  Vue 前端 (3000)      │  │                        │
│  └──────────┬────────────┘  │                        ▼
│  ┌──────────▼────────────┐  │              ┌──────────────────┐
│  │ FastAPI 后端 (8090)   │  │              │   MySQL 8.0      │
│  │  - 主 Agent            │  │              │   (3306)         │
│  └──────────┬────────────┘  │              └──────────────────┘
│  ┌──────────▼────────────┐  │
│  │ MCP Server (8000)     │──┼──▶ 调 Java 后端 REST API
│  └──────────┬────────────┘  │
│  ┌──────────▼────────────┐  │
│  │ Agent Protocol (2024) │  │     异步采购分析子 Agent
│  └──────────┬────────────┘  │
│  ┌──────────▼────────────┐  │
│  │ OpenSandbox 沙箱      │  │     代码执行环境（远程）
│  └───────────────────────┘  │
└─────────────────────────────┘

外部依赖：
  - MongoDB      (39.100.100.28:27017)  — 对话状态/checkpoint 持久化
  - OpenSandbox  (39.100.100.28:8080)   — 代码沙箱
  - 魔搭社区 MCP  (modelscope.net)       — 图表/可视化生成
  - LLM 大模型   (DeepSeek + 智谱 GLM)   — Agent 推理
```

---

## 二、环境要求总表

| 组件 | 版本要求 | 是否必需 | 用途 |
|------|---------|:---:|------|
| **Python** | 3.10+（实测 3.14） | ✅ | 运行 AI 项目后端 |
| **Node.js + npm** | 18+ | ✅ | 运行 Vue 前端 |
| **JDK** | 17+（实测 21） | ✅ | 运行 Java 后端 |
| **Maven** | 3.6+（建议 3.8/3.9） | ✅ | 构建 Java 后端 |
| **MySQL** | 8.0.x | ✅ | Java 后端数据库 |
| **MongoDB** | 任意（远程已部署） | ✅ | Python 状态持久化 |
| **OpenSandbox** | 远程已部署 | ✅ | 代码沙箱 |
| **LLM API Key** | DeepSeek + 智谱 | ✅ | Agent 推理 |
| **魔搭社区 MCP** | 远程服务 | ✅ | 图表生成（无需配置） |

> ⚠️ 注意：README 里写的 "JDK 1.8+、Spring Boot 2.7.18" 是过时文档，以 `pom.xml`（JDK 17 / Spring Boot 3.1.10）为准。

---

## 三、第一部分：Java ERP 后端

### 3.1 环境准备

1. 安装 **JDK 17+**（推荐 21 LTS），配置 `JAVA_HOME`
2. 安装 **Maven 3.6+**，配置 `MAVEN_HOME`
3. 安装 **MySQL 8.0.x**，启动 MySQL 服务

### 3.2 配置数据库

修改 `MotorcyclePartsProcurementSystem/src/main/resources/application.yml`：

```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/motorparts_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root          # ← 改成你的 MySQL 账号
    password: 123456        # ← 改成你的 MySQL 密码
```

> 数据库 `motorparts_db` **无需手动建表**。首次启动时系统会自动建表并插入模拟数据（约 30 供应商 / 60 零部件 / 120 订单）。
>
> 如需手动建库：
> ```sql
> CREATE DATABASE motorparts_db CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
> ```

### 3.3 运行

```bash
cd MotorcyclePartsProcurementSystem
mvn spring-boot:run
# 或打包运行：
# mvn clean package -DskipTests
# java -jar target/*.jar
```

启动成功后：
- 首页：`http://localhost:8080`
- API 文档：`http://localhost:8080/swagger-ui.html`
- API 前缀：`http://localhost:8080/api`

### 3.4 ⚠️ 端口对齐（关键）

Java 后端默认跑 **8080**，但 Python 侧的 MCP Server 默认指向 **8081**：

```python
# erp_harness01/src/mcp_server/server_config.py:4
JAVA_API_BASE_URL = "http://47.92.108.163:8081/api"
```

**二选一，让两边端口一致：**

- 方案 A：让 Java 后端跑 8081 —— `mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=8081`
- 方案 B：改 Python 侧配置为本地 8080 —— 把 `server_config.py` 里的地址改成 `http://127.0.0.1:8080/api`

---

## 四、第二部分：Python AI 项目

### 4.1 环境准备

1. 安装 **Python 3.10+**（推荐 3.11+，作者实测 3.14）
2. 安装 **Node.js 18+**（含 npm）

### 4.2 创建 `.env` 配置文件（必做）

`erp_harness01/` 目录下**没有 `.env` 会启动失败**。请手动创建 `erp_harness01/.env`：

```ini
# ===== 必填：LLM 大模型 =====
# DeepSeek（主模型 deepseek-v4-pro / 摘要模型 deepseek-v4-flash）
DEEPSEEK_API_KEY=sk-你的DeepSeek密钥
DEEPSEEK_BASE_URL=https://api.deepseek.com

# 智谱 GLM（备用模型 glm-5.1）
ZHIPU_API_KEY=你的智谱密钥
ZHIPU_BASE_URL=https://open.bigmodel.cn/api/paas/v4

# ===== 可选：其他模型 =====
# OPENAI_API_KEY=
# MINIMAX_API_KEY=
# ALIBABA_API_KEY=
# K2_API_KEY=
# DAYTONA_API_KEY=
```

> `.env` 已在 `.gitignore` 中忽略，**不要提交到 GitHub**。

### 4.3 安装 Python 依赖

```bash
cd erp_harness01
pip install -r requirements.txt
pip install -U "langgraph-cli[inmem]"
```

核心依赖：`deepagents`、`langgraph`、`fastmcp`、`fastapi`、`uvicorn`、`opensandbox`、`langchain-mcp-adapters`、`langgraph-checkpoint-mongodb` 等。

### 4.4 安装前端依赖

```bash
cd erp_harness01/frontend
npm install
```

### 4.5 启动（一键启动全部 5 个进程）

```bash
cd erp_harness01
python start_web.py
```

启动器会按依赖顺序依次拉起并健康检查：

| 顺序 | 进程 | 端口 | 作用 |
|------|------|------|------|
| 1 | MCP Server | `127.0.0.1:8000/mcp` | 封装 Java 后端业务工具 |
| 2 | OpenSandbox 预热 | — | 预热共享代码沙箱 |
| 3 | Agent Protocol | `127.0.0.1:2024` | 异步采购分析子 Agent |
| 4 | FastAPI 后端 | `0.0.0.0:8090` | 主 Agent + 对话 API |
| 5 | Vue 前端 | `localhost:3000` | 聊天界面 |

> 首次启动 Agent 初始化约需 **30~60 秒**，请耐心等待。全部就绪后会打印 "All services started successfully!"。

### 4.6 访问

- **聊天界面**：`http://localhost:3000`
- **后端 API 文档**：`http://localhost:8090/docs`
- **MCP 端点**：`http://127.0.0.1:8000/mcp`

---

## 五、外部依赖服务说明

以下服务已硬编码在代码里，**默认指向作者已部署的远程服务器**。如果使用作者的环境，无需额外配置；如果自建，需要修改对应代码：

| 服务 | 默认地址 | 写在哪 | 需改的密码/地址 |
|------|---------|--------|----------------|
| MongoDB | `mongodb://root:123456@39.100.100.28:27017/` | `src/agent/config.py:110` 与 `src/api_view/web_config.py:13` | 账号/密码/地址 |
| OpenSandbox | `http://39.100.100.28:8080` | `src/agent/config.py:62`、`src/agent/backends/global_sandbox_manager.py:44` | 沙箱地址 |
| Java 后端 | `http://47.92.108.163:8081/api` | `src/mcp_server/server_config.py:4` | 后端地址 |
| 魔搭 MCP | `https://mcp.api-inference.modelscope.net/af3893df5be041/mcp` | `src/agent/tools/mcp_client.py:24` | 远程服务，一般不用改 |

---

## 六、完整启动顺序

从头到尾把整套系统跑起来的正确顺序：

```text
1. 启动 MySQL（本地服务）
2. 启动 Java ERP 后端（mvn spring-boot:run）→ 确认 8080/8081 可访问
3. 确认远程 MongoDB / OpenSandbox 可连通（或自建并改配置）
4. 创建 erp_harness01/.env（填 API Key）
5. 安装 Python 依赖 + 前端依赖
6. 对齐 Java 后端端口（server_config.py）
7. cd erp_harness01 && python start_web.py
8. 浏览器打开 http://localhost:3000
```

> 依赖关系：Java 后端 → 依赖 MySQL；MCP Server → 依赖 Java 后端；主 Agent → 依赖 MCP + MongoDB + OpenSandbox + LLM；前端 → 依赖 FastAPI 后端。

---

## 七、端口速查表

| 端口 | 服务 | 项目 |
|------|------|------|
| 3306 | MySQL | Java 后端数据库 |
| 8080 | Java ERP 后端（本地默认） | Java |
| 8081 | Java ERP 后端（远程部署地址） | Java |
| 8000 | MCP Server | Python |
| 2024 | Agent Protocol（LangGraph） | Python |
| 8090 | FastAPI 后端 | Python |
| 3000 | Vue 前端（Vite） | Python |
| 27017 | MongoDB（远程） | 外部 |
| 8080 | OpenSandbox（远程） | 外部 |

---

## 八、常见问题排查

### Q1：`python start_web.py` 卡在 "Waiting for MCP..."
- 检查 Java 后端是否已启动、`server_config.py` 的地址/端口是否正确。
- 检查 8000 端口是否被占用。

### Q2：启动报 API Key 相关错误
- 确认 `erp_harness01/.env` 已创建，且 `DEEPSEEK_API_KEY`、`ZHIPU_API_KEY` 已正确填写。

### Q3：Java 后端启动报 "Port 8080 was already in use"
- 8080 被占用。改用其他端口启动：`mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=8081`，并同步改 Python 侧 `server_config.py`。

### Q4：Java 后端连不上 MySQL
- 确认 MySQL 已启动、`application.yml` 里账号密码正确、库名 `motorparts_db` 存在（或让系统自动建）。

### Q5：Agent 初始化失败（MCP 工具加载失败）
- 确认 MCP Server（8000）先于 FastAPI 后端（8090）启动。`start_web.py` 已保证顺序，单独手动启动时注意顺序。

### Q6：上传 GitHub 需要注意什么
- 删除两个子目录内层的 `.git`（否则变嵌套仓库传不上去）。
- 给 `erp_harness01/.gitignore` 补 `node_modules/`、`.idea/`。
- 不要提交 `.env`；代码里的明文密码/IP 若公开请先脱敏。

---

## 附：最小可运行组合

| 场景 | 需要准备 |
|------|---------|
| **只跑 Java 后端** | JDK 17+ + Maven + MySQL 8.0 |
| **只跑 Python AI 项目**（连作者远程服务） | Python 3.10+ + Node.js + `.env` API Key |
| **整套完整跑通** | 上面全部 + MongoDB + OpenSandbox（或改配置指向自建服务） |